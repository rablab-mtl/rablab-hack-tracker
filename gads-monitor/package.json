{
  "name": "rablab-gads-monitor",
  "version": "1.0.0",
  "private": true,
  "description": "Module 2 du rablab-hack-tracker : worker Cloudflare qui surveille le MCC Google Ads et sert la config des agents endpoint.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.5.0",
    "wrangler": "^4.0.0"
  }
}
